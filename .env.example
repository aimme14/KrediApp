# Copia a .env.local y rellena. Reinicia `npm run dev` tras cambiar variables NEXT_PUBLIC_*.

# Firebase Web Push (obligatorio para FCM en el navegador del admin)
# Firebase Console → ⚙️ Configuración del proyecto → Cloud Messaging → Certificados de Web Push
NEXT_PUBLIC_FIREBASE_VAPID_KEY=
